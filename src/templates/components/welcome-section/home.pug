section.home
    .container.home__container
        .home__btns
            .scroll
                a(href='javascript:void(0);').scroll__btn Скролл
                    +icon('scroll', 'scroll__icon')